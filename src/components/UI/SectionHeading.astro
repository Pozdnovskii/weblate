---
const { title, id } = Astro.props;
---

<h2
  id={id}
  class="heading-mono mb-6 max-lg:flex max-lg:justify-between lg:grid-wrapper__subgrid"
>
  <span aria-hidden="true">/</span><span>{title}</span>
</h2>
